Scripts associated with UTCS and UT Austin in general.

These scripts are not useful for other applications.

These scripts must be aliased from a dotfile since they are not added to the
system path by default.
